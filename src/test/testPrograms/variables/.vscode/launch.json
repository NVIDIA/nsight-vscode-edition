{
   "version": "0.2.0",
    "configurations": [
        {
            "name": "CUDA: Debug with CUDA-GDB",
            "type": "cuda-gdb",
            "request": "launch",
            "gdb": "cuda-gdb",
            "program": "${workspaceFolder}/variables",
            "logFile": "${workspaceFolder}/.rubicon_log",
            "verboseLogging": true,
            "testMode": true
        }

    ]
}
